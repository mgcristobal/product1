{
  "name": "i18n-mongodb-native-driver",
  "version": "1.0.0",
  "description": "Demo: require('i18n') with MongoDB native driver (no Mongoose)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "i18n": "^0.15.1",
    "mongodb": "^6.3.0"
  }
}
